type NotificationGroup = 'NotificationGroup';
type NotificationReaded = 'NotificationReaded';
type Notifications = 'Notifications';

export type collections = NotificationGroup | NotificationReaded | Notifications;
